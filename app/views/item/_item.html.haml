.box__pickup
  %h2.box__pickup__category_title
    ピックアップカテゴリー
  .items
    .itemslists
      - @items.each do |item|
        .itemlist
          = link_to item_path(id: item.id), class: "itemlist__link" do
            = image_tag item.images.first.image_url.url, class: "itemlist__image"
            %h3.name
              = item.name
          .itemlist__details
            %ul
              %li.itemlist__details-price
                = item.initial_price
                = "円"